# AGENTS.md

이 문서는 AI 에이전트가 가장 먼저 읽는 진입점이다. `{{PROJECT_NAME}}` 플레이스홀더를 채워 그대로 사용한다.

## 프로젝트 개요

`{{PROJECT_NAME}}`는 `{{PROJECT_DESCRIPTION}}`를 목표로 하는 프로젝트다.

## 문서 우선순위

1. 사용자의 명시 요청
2. 이 파일의 지침
3. `docs/harness/TASK_PLAN.md`의 현재 작업 범위와 완료 기준
4. `.agents/**/*.{md,mdc}`의 에이전트 작업 규칙
5. `.rules/**/*.mdc`의 상세 개발 규칙
6. `.skills/**/*.{md,mdc}`의 작업별 스킬 지침
7. `PRD.md`의 제품 요구사항
8. 기존 코드 컨벤션

`TASK_PLAN.md`가 위에 있는 것은 우선순위가 아니라 관할 범위 때문이다. **무엇을 어디까지** 만들지는 `TASK_PLAN.md`가, **어떻게** 만들지는 `.rules`가 정한다. 둘이 충돌하면 임의로 결정하지 말고 사용자에게 확인한다.

## 공통 AI 문서 경로

이 프로젝트는 특정 AI 도구에 종속되지 않도록 루트 경로의 공통 폴더를 기준으로 한다.

- `.agents`: AI 에이전트의 작업 방식, 응답 방식, 검증 흐름 등 실행 지침
- `.rules`: 아키텍처, 네이밍, API, 테스트, 리뷰 등 프로젝트 개발 규칙
- `.skills`: 특정 작업 유형별 절차와 참고 지식
- `PRD.md`: 제품 요구사항과 MVP 범위, 설계 결정 기록(ADR), 작업 로드맵
- `docs/TECH_STACK.md`: 기술 스택, 배포 환경, 검증 커맨드
- `docs/harness`: 현재 단위 작업의 플랜·결과·리뷰 상태 문서

확장자 규칙: 규칙 문서는 `.mdc`, 하네스 상태 문서는 `.md`를 사용한다. 매 작업마다 내용이 교체되는 상태 문서가 규칙으로 오인되어 컨텍스트에 상시 주입되는 것을 막기 위해서다.

도구별 자동 인식이 필요한 경우 `.cursor/`, `.claude/`, `.codex/` 등에는 위 공통 문서를 참조하는 얇은 연결 문서만 둔다.

## 상세 규칙

코드 작성, 리뷰, 테스트, 아키텍처 판단은 `.rules` 하위 문서를 따른다.

- 아키텍처/FSD·상태 관리: `.rules/architecture.mdc`
- 기술 스택·네이밍·주석·코드 품질: `.rules/coding-standards.mdc`
- API 계약/응답/에러 처리/MSW: `.rules/api-standards.mdc`
- UI·디자인 토큰·공통 컴포넌트: `.rules/ui-standards.mdc`
- 테스트 정책: `.rules/testing.mdc`
- Git 컨벤션: `.rules/git-convention.mdc`
- 에이전트 작업 흐름·리뷰 기준: `.rules/agent-workflow.mdc`

## 에이전트 지침

AI 에이전트별 공통 작업 지침은 `.agents` 하위 문서를 따른다. 신규 에이전트 규칙을 추가할 때는 특정 도구 이름보다 역할과 적용 범위를 기준으로 파일을 작성한다.

## 스킬 지침

반복 가능한 작업 절차나 특정 도메인 지식은 `.skills` 하위 문서로 분리한다. 예를 들어 문서 작성, Figma 기반 UI 구현, API 명세 반영, 테스트 작성처럼 독립적인 절차가 있는 작업을 스킬 문서로 관리한다.

## 작업 하네스

여러 세션·모델이 계획, 구현, 검증을 나눠 맡는다. 이전 대화 기억에 의존하지 않고 `docs/harness` 문서만으로 작업을 이어갈 수 있어야 한다. 운영 규칙 전문은 `docs/harness/PROCESS.md`를 따른다.

`docs/harness`가 아직 없으면 `docs/template-harness-docs-v2`에서 `PROCESS.md`, `TASK_PLAN.md`, `TASK_RESULT.md`, `REVIEW.md`를 복사해 만든다. 템플릿 원본은 수정하지 않는다.

### 문서 소유권

| 문서 | 역할 | 담당 |
| --- | --- | --- |
| `docs/harness/TASK_PLAN.md` | 현재 단위 작업 범위 + 완료 기준(AC) | 계획·리뷰 에이전트 |
| `docs/harness/TASK_RESULT.md` | 작업 결과 보고 + 중단 시 체크포인트 | 구현 에이전트 |
| `docs/harness/REVIEW.md` | 심각도 기반 리뷰 판정 | 계획·리뷰 에이전트 |
| `PRD.md` | 제품 목표, ADR, 로드맵 | 계획 에이전트 |

자기 소유가 아닌 문서는 읽기만 한다. 구현 에이전트가 `REVIEW.md`의 판정을 직접 고치지 않는다.

### 구현 에이전트 규칙

`TASK_PLAN.md`를 읽고 구현한 뒤, **결과 보고 전에 아래를 순서대로 통과시킨다.** 커맨드는 `docs/TECH_STACK.md`의 검증 커맨드 표를 따른다.

1. 타입 검사 통과
2. 린트 통과
3. 테스트 통과 (버그 수정이면 재현 실패 테스트를 먼저 작성해 Red를 확인한 뒤 수정)
4. 자체 동작 검증

그 다음 `TASK_RESULT.md`를 정해진 항목만 채워 작성한다. 산문 서술은 쓰지 않는다. diff를 읽으면 알 수 있는 내용(어떤 파일을 어떻게 고쳤는지)은 생략하고, **diff에서 읽을 수 없는 것**만 기록한다.

- 검증 증거: 실행한 커맨드와 실제 결과
- 의도적 결정: diff만 보면 규칙 위반으로 오해할 수 있는 선택과 그 근거
- 미해결 사항: 발견했으나 이번 범위에서 처리하지 않은 것

검증을 실행하지 않았으면 실행했다고 쓰지 않는다. 실패한 검증은 숨기지 않고 기존 오류와 신규 오류를 구분해 기록한다.

### 리뷰 에이전트 규칙

판단 근거의 우선순위는 다음과 같다.

1. `git diff` — 실제 코드
2. `TASK_PLAN.md`의 완료 기준(AC)
3. `TASK_RESULT.md`의 보고 내용

결과 보고서는 자기 작업에 대한 자기 서술이라 낙관 편향이 있다. 보고된 주장이 diff나 검증 증거로 뒷받침되지 않으면 미검증으로 판정한다.

지적사항에는 심각도를 반드시 부여한다.

- **Blocker**: 기능 오동작, 데이터 손상, AC 미충족 — 수정 없이 커밋 불가
- **Major**: 아키텍처 규칙 위반, 명백한 버그 가능성, 테스트 누락 — 수정 필요
- **Minor**: 네이밍, 주석, 사소한 스타일 — 라운드를 돌리지 않고 이월

검증 항목 자체는 `.rules/agent-workflow.mdc`의 코드 리뷰 검증 목록을 따른다.

### 라운드 관리

- Blocker/Major가 남아 있으면 해당 항목만 수정하고 재검증한다.
- Minor만 남으면 PASS 처리하고 `REVIEW.md`의 이월 목록에 기록한다.
- **리뷰 3라운드를 넘기거나 같은 항목이 두 번 왕복하면 루프를 멈추고 사용자에게 판단을 요청한다.**
- PASS 후 커밋하면 `TASK_RESULT.md`와 `REVIEW.md`를 `docs/harness/archive/{issue-code}/`로 옮기고 본 문서는 비운다. 이후에도 알아야 할 결정은 `PRD.md`의 설계 결정 기록으로 승격시킨다.

## 제품 기준

제품 요구사항과 MVP 범위는 `PRD.md`를 따른다. 구현 중 제품 방향과 코드 규칙이 충돌하면 사용자에게 확인한다.
