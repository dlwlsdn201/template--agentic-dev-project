# TASK_PLAN — 단위 작업 플랜

> 소유: Codex (설계 세션) · 소비: Claude Code (구현 세션)
> 원칙: **현재 단위 작업만** 기술. 완료된 작업은 이 문서에서 삭제(이력은 git·아카이브).
> 구현 방법의 세부는 최소화 — 최신 모델은 코드베이스를 직접 탐색하므로,
> "무엇을·왜·어디까지"에 집중하고 "어떻게"는 제약 조건만 명시한다.

---

## 작업 ID
<GITLAB 이슈 코드 및 제목. 예: #142 그룹 장치 일괄 제어 기능>

## 배경 / 목적 (왜)
<PRD의 어느 목표에 해당하는지 1~3줄. PRD 섹션 링크>

## 작업 범위 (무엇을)
- [ ] <단위 작업 1 — 파일/모듈 단위로 구체적으로>
- [ ] <단위 작업 2>

### 범위 제외 (Out of Scope)
- <이번 작업에서 건드리지 않을 것 — 리뷰 시 scope creep 판정 기준>

## 제약 조건 (어떻게의 경계)
- <아키텍처 제약. 예: entities hook은 invalidateQueries만, toast는 features에서>
- <프로젝트 규칙 문서 중 이번 작업에 특히 해당하는 조항 참조>

## 완료 기준 (Acceptance Criteria) ★ 리뷰 판정의 기준
> 리뷰어는 아래 항목 각각에 대해 PASS/FAIL을 판정한다.
> 검증 가능한 서술로 작성 (측정·재현 가능해야 함).

- [ ] AC-1: <예: 그룹 선택 후 제어 버튼 클릭 시 확인 모달이 뜨고, 확인 시 API가 group_id 배열로 호출된다>
- [ ] AC-2: <예: API 실패 시 NormalizedApiError.message가 toast로 노출된다>
- [ ] AC-3: 기계적 게이트 — tsc, lint, test 전부 통과
- [ ] AC-4: <테스트 요구 — 예: entities hook 통합 테스트(MSW, 에러 응답 포함) 추가>

## 예상 리스크 / 열린 질문
- <설계 시점에 확신 없는 부분. 구현자가 발견 시 TASK_RESULT '미해결 사항'에 기록>

## 실제 코드 작업 에이전트가 필수로 지켜야할 사항
- 작업 완료 전 tsc·lint·test 필수
- TASK_RESULT는 템플릿 항목만 채울 것