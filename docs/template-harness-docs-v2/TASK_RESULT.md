# TASK_RESULT — 작업 결과 보고 (+ 체크포인트)

> 소유: Claude Code (구현 세션) · 소비: Codex (리뷰 세션), 후속 세션
> 원칙: **산문 서술 금지, 정형 항목만.** 코드로 알 수 있는 내용(무슨 파일을 어떻게
> 고쳤는지의 세부)은 쓰지 않는다 — 리뷰어는 git diff를 직접 읽는다.
> 이 문서는 "diff에서 읽을 수 없는 것"만 담는다: 검증 증거, 의도적 결정, 미해결 사항.
> 커밋 완료 시 /docs/archive/{issue-code}/로 이동 후 본 문서 비움.

---

## 작업 ID
<TASK_PLAN의 작업 ID와 동일>

## 상태
<완료 | 진행중(체크포인트 참조) | 블록됨(사유)>

## 라운드
<1차 구현 | n차 수정 (REVIEW 어느 항목 반영인지: 예 "2차 — B-1, M-2 반영")>

## AC 자체 판정
| AC | 자체 판정 | 근거 (한 줄) |
|---|---|---|
| AC-1 | PASS | <검증 방법 요약> |
| AC-2 | PASS | |
| AC-3 | PASS | 아래 검증 증거 참조 |

## 검증 증거 ★ 필수 — 커맨드와 실제 출력 요약을 붙일 것
```
$ pnpm tsc --noEmit   → 통과 (0 errors)
$ pnpm lint           → 통과
$ pnpm test           → 통과 (예: 42 passed, 신규 테스트 3개 포함)
```
- E2E/수동 검증: <무엇을 어떤 절차로 확인했는지. "확인함" 금지 — 재현 가능하게>

## 변경 파일 목록
<git diff --stat 수준. 상세 설명 불필요>

## 의도적 결정 사항 (diff만 봐서는 오해할 수 있는 것)
- <예: useBaseQuery를 쓴 이유 — NewGridLayout 자체 Suspense 예외 조항 해당>

## 미해결 사항 / 발견한 리스크
- <구현 중 발견했으나 이번 범위에서 처리하지 않은 것. 없으면 "없음">

---

## 체크포인트 (작업이 중간에 끊긴 경우에만 작성, 완료 시 삭제)
- 마지막 완료 지점: <>
- 이어서 할 일 (순서대로): <>
- 주의 사항 / 현재 세션에서만 알 수 있는 맥락: <>
